# Linux / Shell 命令笔记

给自己复习用，整理部署这段实际用到的 Linux 命令，按场景分类。跟 `api-service/NOTES.md`（C#概念）、`DEPLOYMENT.md`（AWS架构）是同一系列，这份专门管"敲的这些命令到底是什么意思"。

## 连接服务器（SSH）

```bash
ssh -i "密钥文件路径.pem" ubuntu@公网IP
```
- `ssh`：远程登录程序，建立一个加密的远程终端连接。
- `-i "路径"`：指定用哪把私钥文件证明身份（identity file），代替密码登录——AWS EC2 默认就是这种"钥匙"认证方式，没配密码登录。
- `ubuntu@IP`：`用户名@目标地址`，`ubuntu` 是这台服务器上的系统用户名（AWS 官方 Ubuntu 镜像默认用户）。

```bash
exit
```
退出当前 SSH 会话，回到本地终端。**注意**：这只是断开"你和服务器的连接"，服务器本身和上面跑的容器完全不受影响，继续在后台运行。

SSH 会话意外断开（网络波动、电脑休眠、太久没操作超时）也是同理，重新执行一遍 `ssh -i ...` 那条命令连回去就行，服务器状态不会丢。

## 文件/目录导航

| 命令 | 作用 |
|---|---|
| `ls` | 列出当前目录下的文件/文件夹 |
| `ls -la` | `-l` 详细信息（权限、大小、修改时间），`-a` 连隐藏文件（`.` 开头的，比如 `.env`）也显示 |
| `cd 目录名` | 切换目录（Change Directory） |
| `cd ~` 或 `cd` （不带参数） | 回到当前用户的主目录（`/home/ubuntu`） |
| `pwd` | 打印当前所在的完整路径（Print Working Directory），迷路了就用这个 |
| `tree -L 2` | 树状展示目录结构，`-L 2` 限制只展开 2 层（默认没装，要先 `sudo apt install -y tree`） |

**新开一个 SSH 会话，默认停在主目录 `~`，不是项目目录**——每次重新连上服务器，记得先 `cd nz-market-insights` 再干活。

## 权限与 `sudo`

- **`sudo`**：不是"一种操作语言"，是一条命令，意思是"用管理员（root）权限执行紧跟在后面的这一条命令"，等价于 Windows 的"以管理员身份运行"。普通用户默认权限有限（比如装软件、改系统级配置都不行），加 `sudo` 临时提权。
- **`chmod`**：改文件权限（change mode），部署时用过 `sudo chmod a+r 文件` 给 Docker 的 GPG 密钥文件加"所有人可读"权限。
- 判断要不要加 `sudo` 的经验规律：凡是"装软件"“改系统配置文件"“操作不属于当前用户的文件"，大概率需要；查看自己创建的文件、进自己的项目目录，不需要。

## 包管理（apt，Ubuntu 的软件包工具）

| 命令 | 作用 |
|---|---|
| `sudo apt update` | 刷新"有哪些软件包可以装"这份清单，不是真的装东西，装新软件前一般先跑这条 |
| `sudo apt install -y 包名` | 安装软件包，`-y` 表示自动对所有确认提示回答"是"，不用手动按 y 回车 |

## 文本编辑（nano）

```bash
nano 文件名
```
打开一个终端里的简易文本编辑器（新文件不存在的话会新建）。常用快捷键：
- `Ctrl+O`：保存（write Out），会让你确认文件名，直接回车确认
- `Ctrl+X`：退出

用来在服务器上直接创建/编辑 `.env` 这类配置文件，不用先在本地写好再传上去。

## Docker 相关

| 命令 | 作用 |
|---|---|
| `docker --version` / `docker compose version` | 确认安装成功、看版本号 |
| `docker ps` | 列出**正在运行**的容器 |
| `docker ps -a` | 列出**所有**容器，包括已经停止/退出的（`-a` = all）——排查"为什么这个容器没起来"必用 |
| `docker compose up -d --build` | 根据 `docker-compose.yml` 启动所有服务；`-d` 后台运行（不占着终端）；`--build` 先按 Dockerfile 重新构建镜像，代码有改动时才需要加 |
| `docker compose logs 服务名` | 看某个服务的运行日志/报错信息 |
| `docker compose run --rm 服务名` | 临时起一个容器跑一次性任务（不是常驻服务），跑完用 `--rm` 自动删除这个容器，不留垃圾——cron 定时刷新数据用的就是这个，不是 `up` |
| `which docker` | 查一个命令的完整安装路径（比如确认 `docker` 到底装在 `/usr/bin/docker`），写 cron 任务时需要用绝对路径，不能只写命令名 |

## Git（大部分在本地用，服务器上主要是这两条）

```bash
git clone 仓库地址     # 第一次把代码拉下来
git pull                # 拉取最新提交，更新已有的本地副本
```

## 定时任务（cron）

| 命令 | 作用 |
|---|---|
| `crontab -e` | 编辑当前用户的定时任务列表（第一次用会让你选编辑器，选 `nano` 最简单） |
| `crontab -l` | 列出（list）当前用户配置了哪些定时任务，用来确认保存成功、格式对不对 |

**cron 时间字段的顺序，固定是 5 个，从左到右**：
```
分 时 日 月 星期   命令
0  3  */3 *  *    cd /path && command
```
- 每个字段可以填具体数字，也可以填 `*` 表示"不限制/任意"
- `*/3` 这种写法表示"能被 3 整除"，用在"日"这一位就是"每隔 3 天"
- cron 是**服务器系统自带的常驻调度程序**，不需要你的 SSH 会话开着，退出登录后它照样按时触发

## 其他实用命令

| 命令 | 作用 |
|---|---|
| `curl -fsSL 网址 -o 文件名` | 从网上下载文件保存到本地。`-f` 出错不显示网页内容、`-s` 静默不显示进度条、`-S` 出错时仍显示错误信息、`-L` 自动跟随重定向 |
| `curl 网址` | 直接访问一个 URL 并把返回内容打印出来，部署时用来测试 API 接口有没有正常返回数据（不用打开浏览器） |

## Shell 里几个常见符号

| 符号 | 含义 | 例子 |
|---|---|---|
| `&&` | 前一条命令**成功**（退出码为0）才执行后面这条 | `cd 目录 && docker compose up` —— 切目录失败就不会误跑后面的命令 |
| `>>` | 把输出**追加**写入文件（不覆盖已有内容） | `... >> 日志.log` |
| `>` | 把输出**覆盖**写入文件（会清空原有内容，要小心用） | 跟 `>>` 的关键区别是会不会清空旧内容 |
| `2>&1` | 把"错误输出"也重定向到跟"正常输出"一样的地方 | 配合 `>>` 用，保证报错信息也被记进日志文件，不会漏掉 |
| `-y` | 很多命令的通用参数，表示"自动确认所有提示，不用手动按 y" | `apt install -y` |